services:
  app:
    image: docker.io/dnomd343/syncplay:latest
    restart: always
    ports:
      - 8999:8999
    environment:
      TZ: ${TZ:-Etc/UTC}
      PASSWORD: ${PASSWORD}
    networks:
      - network
networks:
  network:
    enable_ipv6: true
